General
 - Figure out how the checksum of the config record works, goal, change the config
   record to use full ram and enable the use of floppy disk and to enable 2 wd controllers
   DONE: Enrique has figured it out and wrote a python sccript edit the config record
 - Make a new image that contains the diags on wd (required 2 nd disk support)
   DONE partly, 2 disk support works
 - Get rid of personal data from Scott Printing from the image but leaving the basic programs working
 - Test asm, may work
   DONE: Enrique has figured it out, does not work, assembles but does not write the output due to missing kernel function
 - Get a linker/c-compiler/libs/headers, the only one i have is for a newer version of unos, traps with unknown trap,
   same for c compiler and header files
 - Complete and test the installation tape further, try if instetc from the 3000 tape can be patched to run even
   it was compiled for 68020

emu
 Move sock/scc polling and fw int dequeue to sys_device_tick
 PARTLY DONE: poll is now in a separate thread
 DONE AD Sep 3,2026 Add nvram load command
 Add command to post keystrokes to sock
 DONE: Add exec command to allow auto start of several xterm's with telnet after sock was initialized
 Add a command to save messages to a file as well
 DONE AD Sep 3,2026 dont write no color escape sequences when colors are off
 Write the message w/o disassembly on recusive call intead of suppressing the message
 Support commands in user mode +option to override to use absolute addresses
 DONE Windows port

fway
 DONE AD Sep 3,2026 Fix interrupt message
 DONE Implement character input 
 Implement reset
 Implement registers
 Make more diag tests running
 MDONE ay be diag will check status after a command, in that case all ints have to queued and fired after n instructions
 Fix paste

wd
 DONE Add second harddisk support
 DONE Check if we can create a new image with diag (format support may be needed)
 DONE make more tests pass (passed all standard tests except ram parity)
 Check/fix support for 2nd wd
 DONE implement format to create images with dutil

sock
 Add support for injecting chars
 DONE Outgoing key translation (e.g. for MB I..MB VI keys required as well as backspace (7f to ^H))
 Add support for serial ports and local sockets
 DONE Make the basic EDIT command work
 printer support

fd
 To be implemented further

cs
 during install trestore fails for the config record
   trestore d=/dev/cs name=CONFIG
   Part of file '/sys/config.2000-97894' missing on tape
   Tape read error -85. Can't restore /sys/config.2000-97894

Image
 Update the menu to contain all programs (games, user progs, ...)
 Reintegrate what was on the original wd0
 Transfer what i have on 5 1/4" floppies from the S/10 to the image

